VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public backend base URL (without /api is also fine). Optional when VITE_USE_SUPABASE_DIRECT=true.
VITE_API_URL=https://your-backend.onrender.com

# Optional: set to true to run login/data directly against Supabase (no separate backend required in production)
VITE_USE_SUPABASE_DIRECT=true

JWT_SECRET=change-this-secret-in-production
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
